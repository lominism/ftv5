export default function Dashboard() {
  return <div>Dashboard</div>;
}

// This page doesn't do anything. I was originally going to have a
// separate dashboard page, but this is now part of the main page and is
// in the main page.tsx file. Just leaving this here for now.
